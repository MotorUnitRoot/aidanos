# Patterns

A pattern is a screen built from the components. Door, Today, and Plan are the product. Last-mile screens are the same parts, written as files in the vault.

Open the HTML files in a browser. They load `../foundations/tokens.css`.
