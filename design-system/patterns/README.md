# Patterns

A pattern is a screen built from the components. Door, Today, and Plan are the product. Last-mile screens are the same parts, written as files in the vault. Patterns assemble primitives. They do not invent a look.

Open the HTML files in a browser. They load `../foundations/tokens.css` and `../components/primitives.css`.
